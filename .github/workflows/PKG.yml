name: P-Bit BE Build/Test Workflow
on:
  push:
    branches:
      - 'main'
  pull_request:
    branches:
      - 'main'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      # - name: Run tests with secrets as environment variables
      #   env:
      #     DB_HOSTNAME: ${{ secrets.DB_HOSTNAME }}
      #     DB_PORT: ${{ secrets.DB_PORT }}
      #     DB_USER: ${{ secrets.DB_USER }}
      #     DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
      #     DB_DATABASE: ${{ secrets.DB_DATABASE }}
      #     SECRET_KEY: ${{ secrets.SECRET_KEY }}
      #     ALGORITHM: ${{ secrets.ALGORITHM }}
      #     ACCESS_TOKEN_EXPIRE_MINUTES: ${{ secrets.ACCESS_TOKEN_EXPIRE_MINUTES }}
      #   run: |
      #     pytest
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Configure CA certificate for Docker registry
        run: |
          mkdir -p /etc/docker/certs.d/13.239.216.36
          echo "${{ secrets.REGISTRY_CA_CERT }}" | base64 -d > /etc/docker/certs.d/13.239.216.36/ca.crt
          update-ca-certificates
      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: 13.239.216.36/p-bit-be:${{ github.sha }}